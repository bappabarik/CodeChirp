import { Container, Sidebar2List } from '@/components';
import React from 'react';

const Draft = () => {
    return (
        <Container>
            <Sidebar2List items={[]}/>
        </Container>
    );
}

export default Draft;
